project_id = "UPDATE" // update with project id
region = "UPDATE"  // update with region
bigquery_region = "UPDATE" // update with bigquery region
parsers = ["UPDATE"] // update with list of data sources